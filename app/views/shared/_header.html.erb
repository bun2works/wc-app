<nav class="navbar sticky-top navbar-expand-md navbar-dark py-0 nav--border">
  <div class="container">
    <%# ロゴ部分 %>
      <a class="navbar-brand" href="http://neezexp.sakura.ne.jp/wcHp/wcTop.html" target="aboutblank"><img src="/assets/wcLogo.png" alt="navber"></a>
    <%# メニュー %>
    <div class="justify-content-end">
      <ul class="navbar-nav">
        <li class="nav-item active my-auto">
          <% if team_signed_in? %>
            <%= link_to team_path(current_team.id), class: "nav-link mr-4" do %>
              <i class="fas fa-users fa-lg"></i> <%= current_team.name %>
            <% end %>
          <% else %>
            <%= link_to new_team_session_path, class: "nav-link mr-4" do %>
              <i class="fas fa-user-times fa-lg"></i> ログイン
            <% end %>
          <% end %>
        </li>
        <li class="nav-item">
          <%= search_form_for @q ,class: "nav-link form-inline mb-0" do |f|  %>
            <%= f.search_field :title_or_team_name_or_team_location_or_comments_body_cont, class: "form-control", placeholder: "検索" %>
          <% end %>
        </li>
      </ul>
    </div>
  </div>
</nav>
