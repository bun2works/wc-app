<nav class="navbar sticky-top navbar-expand-md navbar-dark py-0 nav--border">
  <div class="container">
    <%# ロゴ部分 %>
      <a class="navbar-brand" href="http://neezexp.sakura.ne.jp/wcHp/wcTop.html" target="aboutblank"><img src="/assets/wcLogo.png" alt="navber"></a>
    <%# メニュー格納時のボタン %>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <%# メニュー %>
    <div class="collapse navbar-collapse justify-content-end" id="navbarSupportedContent">
      <ul class="navbar-nav">
        <li class="nav-item active my-auto">
          <% if team_signed_in? %>
            <%= link_to edit_team_registration_path(@team), class: "nav-link mr-4 fa--light" do %>
              <i class="fas fa-users fa-lg"></i> <%= current_team.name %>
            <% end %>
          <% else %>
            <%= link_to new_team_session_path, class: "nav-link mr-4 fa--light" do %>
              <i class="fas fa-user-times fa-lg"></i> ログイン
            <% end %>
          <% end %>
        </li>
        <li class="nav-item active">
          <%= search_form_for @q ,class: "nav-link form-inline mb-2 mb-lg-0" do |f|  %>
            <%= f.search_field :title_or_team_name_or_comments_body_cont, class: "form-control", placeholder: "検索" %>
          <% end %>
        </li>
      </ul>
    </div>
  </div>
</nav>
